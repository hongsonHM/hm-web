import React from "react";
import { Collapse } from "antd";

const { Panel } = Collapse;


function SelectClientStep(props) {
  const [subDivision, setSubDivision] = useState();

  return (
    <div></div>
  );
}

export default SelectClientStep;
